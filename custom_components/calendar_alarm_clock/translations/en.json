{
  "config": {
    "step": {
      "user": {
        "title": "Calendar Alarm Clock Setup",
        "description": "Select the calendar to use for storing alarms. Make sure you have a calendar integration (like CalDAV) set up first.",
        "data": {
          "calendar_entity": "Calendar"
        },
        "data_description": {
          "calendar_entity": "The calendar where alarm events will be stored"
        }
      }
    },
    "abort": {
      "already_configured": "This calendar is already configured for alarm clock."
    },
    "error": {
      "cannot_connect": "Failed to connect to the calendar."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Alarm Clock Options",
        "description": "Configure default alarm settings.",
        "data": {
          "default_snooze_duration": "Default Snooze Duration (minutes)",
          "default_alarm_timeout": "Default Alarm Timeout (minutes)",
          "default_max_snoozes": "Default Max Snoozes"
        },
        "data_description": {
          "default_snooze_duration": "How long to snooze when user hits snooze (default: 9 minutes)",
          "default_alarm_timeout": "How long an alarm rings before timing out (default: 30 minutes)",
          "default_max_snoozes": "Maximum number of snoozes allowed (0 = infinite)"
        }
      }
    }
  }
}

